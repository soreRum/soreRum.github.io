---
title: Detection Rule Challenges - Suricata and Sigma
---

## Suricata Challenge: Detect Telegram API Connection Activity

**Goal**

Create a rule that will detect the packets related to Telegram API communication.

**Rule**

```suricata
alert tls any any -> any any (
    msg:"Detected Telegram API connection Activity -- via Client Hello";
    flow:to_server;
    tls.sni;
    content:"api.telegram.org";
    nocase;
    sid:100001;
    rev:1;
)
```

**Thoughts**

It’s as simple as targeting **tls.sni**, the Server Name Indication.

I originally included the JA3 hash, but realized that it wasn’t helpful for detecting Telegram connection activity. JA3 fingerprints the **client TLS handshake**, not the **destination** (`api.telegram.org`). Adding a JA3 hash for detection would focus on the TLS client implementation rather than Telegram API traffic itself.

JA3 is more useful when detecting unusual client behavior or malware tooling where the TLS fingerprint is unique enough to identify a specific malicious client.

**Key takeaway**

- JA3 → destination independent  
- SNI → more direct and reliable indicator

The challenge expected detection of **12 packets/alerts** matching the target.

![telegram_rule](./images/telegram_rule.png)

---

## Suricata Challenge: Identify Speech Recognition Chrome Extension Payload in Network Traffic

**Goal**

Analyze the captured traffic and determine whether a Chrome extension payload is being transferred. Focus on response content that reveals the extension’s manifest and embedded DLL components, and write a Suricata rule that detects this artifact reliably without relying on IPs or ports.

**Rule**

```suricata
alert http any any -> any any (
    msg:"Speech Recognition Chrome Extension Payload in Network Traffic --- Identified";
    flow:established,to_client;
    http.response_header;
    content:"application/x-chrome-extension";
    file.data;
    content:"Speech Recognition";
    content:"\"manifest_version\": 2";
    content:"\"name\": \"Speech Recognition\"";
    content:"Microsoft.CognitiveServices.Speech.core.dll";
    sid:100002;
    rev:1;
)
```

**Thoughts**

I wanted to build context before creating this rule. I followed the **HTTP/TCP stream** and found a stream where bits of the `manifest.json` file showed up within the transferred bytes, including the string:

```
"name": "Speech Recognition"
```

The host string:

```
msedge.b.tlu.dl.delivery.mp.microsoft.com
```

appears to be a **Microsoft Edge CDN endpoint**, likely used for extension downloads or update packages.

Here is the request example observed in the PCAP:

```
GET /filestreamingservice/files/2132f61f-f790-4ae6-a355-8cf9a1533800?P1=1757185982&P2=404&P3=2&P4=c1pT928%2fcNpn%2bhiPV%2feDJwgcmMghYDFStIXCZMBx0quHbu8wHa63x0JtAf%2fkqoVZhaG0phaVZr5%2fmgTnCtpSRw%3d%3d HTTP/1.1
```

Observations:

- **filestreamingservice** → backend service route  
- **files** → resource type  
- **2132f61f-f790-4ae6-a355-8cf9a1533800** → likely file identifier for the extension payload  
- **P1–P4 parameters** → appear to be tokenized or cryptographically signed request parameters (P4 resembles base64)

The HTTP response content type:

```
Content-Type: application/x-chrome-extension
```

The transfer uses a byte-range request:

```
Range: bytes=1120-2460
```

The **206 Partial Content** response code signals a chunked transfer. This appears consistent with how **BITS (Background Intelligent Transfer Service)** performs downloads.

The HTTP stream also shows binary ZIP markers (`PK`) and embedded extension artifacts such as:

- `manifest.json`
- `Microsoft.CognitiveServices.Speech.core.dll`

Example stream fragment:

```
PK...
manifest.json{"manifest_version": 2,"name": "Speech Recognition","version": "1.15.0.1"}
PK...
Microsoft.CognitiveServices.Speech.core.dll
```

Given the observed traffic, this appears to be a **Microsoft CDN delivery of a Chrome extension package**, likely transferred in chunks via BITS.

The user agent confirms this:

```
User-Agent: Microsoft BITS/7.8
```

Part 1 of the goal was confirmed:

> Analyze the captured traffic and determine whether a Chrome extension payload is being transferred.

Part 2 was writing a **reliable Suricata detection rule**.

Artifacts targeted in the rule:

- `Content-Type: application/x-chrome-extension`
- `manifest.json`
- `"manifest_version": 2`
- `"name": "Speech Recognition"`
- `Microsoft.CognitiveServices.Speech.core.dll`

The **version field was intentionally excluded**, since extension versions change frequently.

The challenge expected **1 alert**.

![telegram_rule](./images/chrome_extension.png)

---

## Suricata Challenge: Detect SectopRAT C2 Beacon

**Goal**

The PCAP includes recurring outbound requests from an infected host that follow a consistent structure. Each request uses a distinctive path and includes a parameter with a uniquely formatted value.

The detection must rely on **request structure**, not network indicators like IP or port.

**Rule**

```suricata
alert http any any -> any any (
    msg:"SectopRAT C2 Beacon";
    flow:to_server,established;
    http.method;
    content:"GET";
    http.uri;
    pcre:"/\/wbinjget\?q=[A-Fa-f0-9]{32}(?:&|$)/U";
    classtype:trojan-activity;
    sid:100003;
    rev:1;
)
```

**Investigation Notes**

Observed domain:

```
vishneviyjazz.ru
```

PCAP IP:

```
185.95.159.55
```

- DNS lookup for the A record does not resolve
- Domain appears inactive

Another observed domain:

```
desk-app-now.com
```

Resolves to:

```
15.197.240.20
```

Nameservers:

```
NS1.VERIFICATION-HOLD.SUSPENDED-DOMAIN.COM
NS2.VERIFICATION-HOLD.SUSPENDED-DOMAIN.COM
```

The domain was created in **May 2025** and appears suspicious.

Additional PCAP IPs observed:

```
104.21.24.30
172.67.216.128
```

These only appear during DNS queries.

Another IP seen in HTTP traffic:

```
5.10.250.239
```

Traffic pattern observed:

1. TCP three-way handshake completes successfully
2. HTTP GET request to:

```
/wmglb
```

3. Shortly after, another request appears with a different URI and query parameter:

```
/wbinjget?q=F3175C7E064B74698D8F67EDF4A4FEAA
```

Response:

```
HTTP/1.1 200 OK
```

This same request repeats **~33 additional times**, indicating beaconing behavior.

Client observed:

```
10.8.15.176  →  5.10.250.239
```

**Detection Logic**

The detection rule targets:

- HTTP request method
- URI path
- parameter format

Instead of hardcoding the specific token, the rule matches **32 hexadecimal characters** using regex.

```
/wbinjget?q=<32 hex characters>
```

The PCRE ensures the token is followed by `&` or the end of the URI.

The challenge expected **34 alerts**.

![telegram_rule](./images/rat_rule.png)
